# 📄 AI_PDF_project – AI PDF Document Analyzer

> **AI_PDF_project** – асинхронный FastAPI‑сервис, с внедрением LangChain и LangGraph. Принимает PDF‑документы, автоматически извлекает из них ключевые поля (номер, дата, отправитель, назначение, сумма) с помощью LLM‑моделей (Ollama/Mistral) и сохраняет результаты в PostgreSQL. Всё построено по принципу чистой архитектуры: модели ↔ схемы ↔ сервисы ↔ роутеры.

---

## 📚 Содержание

1. [Описание проекта](#описание-проекта)  
2. [Основные функции](#основные-функции)  
3. [Архитектура и структура репозитория](#архитектура-и-структура-репозитория)  
4. [Настройка окружения (`.env`)](#настройка-окружения-env)
5. [Требования и установка](#требования-и-установка)  
6. [Запуск приложения](#запуск-приложения)
7. [Тестирование](#тестирование)

---

## 📝 Описание проекта

**AI_PDF** – сервис для автоматической обработки финансовых PDF‑документов. Он умеет:

- ✅ Принимать файлы от авторизованных пользователей (роли `manager`, `admin`).  
- 📄 Извлекать чистый текст из PDF через **PyPDF2**.  
- 🤖 Анализировать текст LLM‑моделью (локальная **Ollama** или облачная **Mistral**).  
- 🗃 Сохранять метаданные и путь к файлу в PostgreSQL.  
- 📧 Отправлять письма‑подтверждения регистрации и сброса пароля (SMTP/`aiosmtplib`).  
- 🔐 Управлять доступом через JWT‑токены (access+registration+reset).  

Все операции полностью асинхронные, что позволяет обслуживать несколько одновременных загрузок без блокировки event‑loop.

---

## ✨ Основные функции

| Функция | Описание |
|---------|----------|
| **Регистрация и подтверждение e‑mail** | Пользователь получает токен‑подтверждения и письмо автоматически. |
| **Авторизация** | JWT‑access‑токен со сроком 30 дней (`Bearer <token>`). |
| **Роли и права доступа** | `guest` – базовая, `manager` – загрузка/редактирование своих документов, `supervisor`/`admin` – полный доступ. |
| **Загрузка PDF** | `/documents/upload_local` (Ollama) и `/documents/upload_mistral_online` (Mistral). |
| **Автоматический анализ** | Промпт → запрос к LLM → парсинг JSON → запись в БД. |
| **Просмотр и редактирование** | Список своих документов, детальная информация, PATCH‑обновление полей. |
| **Супервизорский дашборд** | Список всех документов + e‑mail автора. |
| **Управление пользователями** | Профиль, изменение данных, смена пароля, запрос сброса пароля. |
| **Миграции** | Alembic (`alembic/versions/`). |

---

## 🏗 Архитектура и структура репозитория

```text
AI_PDF/
├── alembic/                # миграции + env.py
│   └── versions/
├── app/
│   ├── api/                # роутеры: auth, users, documents
│   ├── core/               # config, security, email sender
│   ├── db/                 # async engine, Base, get_db
│   ├── models/             # ORM‑модели (User, Document, Role, UserRole)
│   ├── schemas/            # pydantic‑схемы (request/response)
│   ├── services/           # бизнес‑логика (UserService, DocumentService, LLM‑анализаторы)
│   ├── utils/              # вспомогательные функции, кастомные исключения
│   └── main.py             # точка входа FastAPI
├── uploads/                # сохраняются загруженные PDF (создаётся автоматически)
├── .env                    # переменные окружения
├── requirements.txt        # зависимости
└── README.md               # <‑‑ Вы сейчас читаете её
```

## 🛠️ Настройка окружения (`.env`)

Создайте в корне проекта файл **`.env`** (пример ` .env_example`).

```dotenv
# ----------------- База данных -----------------
DB_NAME=name_db
DB_USER=postgres
DB_PASSWORD=passwod
DB_HOST=localhost    #localhost / db (для docker)
DB_PORT=5432

# ----------------- JWT -----------------
JWT_SECRET=XxXxXXXxxxXxXXxXXXxxXxxxXxXXXX

# ----------------- Email -----------------
EMAIL_HOST=smtp.yandex.ru
EMAIL_PORT=465
EMAIL_HOST_USER=my@email.net
EMAIL_HOST_PASSWORD=password
EMAIL_USE_SSL=True
EMAIL_USE_STARTTLS=False
EMAIL_SENDER_NAME="AI_PDF Авторассылка"
CONFIRM_BASE_URL=http://localhost:8000/auth/register-confirm
RESET_BASE_URL=https://localhost:8000/reset-password

# ----------------- Папка загрузок -----------------
UPLOAD_ROOT=/var/www/myapp/uploads          # относительный путь от корня проекта

# ----------------- Ollama -----------------
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=gpt-oss:120b-cloud            # название модели, установленной в Ollama

# ----------------- Mistral -----------------
MISTRAL_API_KEY=your_mistral_api_key
MISTRAL_BASE_URL=https://api.mistral.ai
MISTRAL_MODEL=mistral-large-latest
```


### Следующие этапы разработки:
* Внедрение более сложных функций анализа документов с использованием LangGraph
* Внедрение автотестов
* Проверка кода линтером